  redo I/O routines.
    timeouts on connection/read/write
  *   select
  *   poll
  * accept variable length lines
  * buffers for each connection
  !   buffered output
  !   packet drweb commands
    support null in smtp sream
  * socket connect/accept routines
  * fork server

  FSM for smtp
  parsing commands/replys
  parsing extended result codes
* malloc debug
    rewrite some routines (strdup)

logging  
  debug( FACILTY )
config file  
access list
  connect
  recepient
  bounce

bounce
  templates
Received: should be optional
setproctitle
signal handling
cleanup when shutting down
LMTP protocol support
  AUTH suport
